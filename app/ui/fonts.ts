import {Inter} from 'next/font/google';
import { Lusitana } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });
export const lusitana = Lusitana({ subsets: ['latin'], weight:'400' });   //Importamos la fuente Inter de Google Fonts, y despues especificamos el subconjunto de caracteres que queremos usar.